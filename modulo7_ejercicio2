import time

hora = time.strftime('%H')
minutos = time.strftime('%M')

if int(hora) >= 7:
    print("Hora de irse")

else:
    print("Quedan {} horas y {} minutos para irse".format(6- int(hora), 59-int(minutos)))
